/**
 * @deprecated Retained only for historical shot-list prototypes that targeted
 * overlay screens and monitor-aligned camera moves. The active self-playing
 * architecture is implemented by FilmPlayer.
 */
export const TIMELINE_SHOTS = Object.freeze({});

export class TimelineEngine {
  async play() {
    return undefined;
  }

  stop() {}
}
